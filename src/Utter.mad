import Action from "@/Action"
import Guard from "@/Guard"
import Interpreter from "@/Interpreter"
import Machine from "@/Machine"
import ActionReducer from "@/Reducer"
import State from "@/State"
import Stream from "@/Stream"
import Transition from "@/Transition"



// Machine

export eventMap = Machine.eventMap
export complete = Machine.complete
export hasState = Machine.hasState
export name = Machine.name
export events = Machine.events
export state = Machine.state
export rawStates = Machine.rawStates
export destinations = Machine.destinations
export machineIsFinal = Machine.isFinal
export hasEvent = Machine.hasEvent
export createMachine = Machine.create
export findIncomplete = Machine.findIncomplete
export states = Machine.states
// Transition

export toSimplePair = Transition.toSimplePair
export createTransition = Transition.create
export target = Transition.target
export transitionToPair = Transition.toPair
export checkGuard = Transition.checkGuard
export reducers = Transition.reducers
export checkMany = Transition.checkMany
export focus = Transition.focus
export transitionName = Transition.name
export makeComplex = Transition.makeComplex
export transition = Transition.transition
export guards = Transition.guards
export createComplex = Transition.createComplex
export event = Transition.event
export createWithContext = Transition.createWithContext
export createBasic = Transition.createBasic
export actions = Transition.actions
// Interpreter

export fling = Interpreter.fling
export initialControl = Interpreter.initialControl
export step = Interpreter.step
export interpreterState = Interpreter.state
export setContext = Interpreter.setContext
export safeCreate = Interpreter.safeCreate
export machine = Interpreter.machine
export setState = Interpreter.setState
export setMachine = Interpreter.setMachine
export createInterpreter = Interpreter.create
export send = Interpreter.send
export context = Interpreter.context
export noStates = Interpreter.noStates
export applyActionsToContext = Interpreter.applyActionsToContext
export stateFromEvent = Interpreter.stateFromEvent
export stepWithPayload = Interpreter.stepWithPayload
// State

export stateIsFinal = State.isFinal
export createState = State.create
export createFinal = State.createFinal
export transitions = State.transitions
export toStatePair = State.toPair
export nameMatches = State.nameMatches
export stateName = State.name
// Stream

export makeMachineStream = Stream.makeMachineStream
export subscribeToMachine = Stream.subscribeToMachine
export subscribeToStreamWithCollectorAndInitial = Stream.subscribeToStreamWithCollectorAndInitial
// Guard

export createGuard = Guard.create
export check = Guard.check
// ActionReducer

export reduce = Reducer.reduce
// Action
export invoke = Action.invoke
