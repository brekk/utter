import Dict from "Dictionary"
import { Just, Nothing } from "Maybe"
import Set from "Set"

import Study from "Study"

import Machine from "@/Machine"
import State from "@/State"
import Transition from "@/Transition"



Study.report(
  Transition.createWithContext($, "b", [], [], []),
  "createWithContext",
  [#["a", Transition.ComplexTransition(Transition.Transition("a", "b"), [], [], [])]],
)
THAT_ONE_NOISE = Transition.create("connect", "modem")
Study.report(Transition.target, "target", [#[THAT_ONE_NOISE, "modem"]])
Study.report(Transition.focus, "focus", [#[Transition.makeComplex(THAT_ONE_NOISE), "modem"]])
