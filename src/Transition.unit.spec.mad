import Dict from "Dictionary"
import { Just, Nothing } from "Maybe"
import Set from "Set"

import Study from "Study"

import Machine from "@/Machine"
import State from "@/State"
import Transition from "@/Transition"
import T from "@/Types"



Study.report(
  Transition.create($, "b", []),
  "createWithContext",
  [#["a", T.Transition(T.TransitionPath("a", "b"), [])]],
)
THAT_ONE_NOISE = Transition.createRaw("connect", "modem")
Study.report(Transition.focus, "focus", [#[THAT_ONE_NOISE, Just("modem")]])
