import Crypto from "Crypto"
import { Just, Nothing } from "Maybe"
import Random from "Random"



export create = (k) => {
  c = 0
  and = () => {
    c := c + 1
    return c
  }
  gen = () => k ++ "-" ++ show(and())
  return { gen }
}

// export create = where {
//   Just(r) => Random.shuffle([
//
//   ], r)
//   _ => []
// }
