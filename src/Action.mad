import type { Maybe } from "Maybe"



export type Action ctx pay = Action(String -> Maybe pay -> Maybe ctx -> Maybe ctx)
export invoke = (ev, ctx, pay, act) => where(act) {
  Action(call) =>
    call(ev, pay, ctx)
}
