import type { Maybe } from "Maybe"



export type Action ctx pay = Action(String -> Maybe pay -> Maybe ctx -> {})
invoke :: String -> Maybe pay -> Maybe ctx -> Action ctx pay -> {}
export invoke = (ev, ctx, pay, act) => where(act) {
  Action(call) =>
    call(ev, pay, ctx)
}
