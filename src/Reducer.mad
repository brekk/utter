import type { Maybe } from "Maybe"



export type ActionReducer pay ctx = ActionReducer(String -> pay -> Maybe ctx -> Maybe ctx)
// reduce :: String -> Maybe ctx -> pay -> ActionReducer ctx pay -> Maybe ctx
export reduce = (ev, pay, ctx, act) => where(act) {
  ActionReducer(call) =>
    call(ev, pay, ctx)
}
