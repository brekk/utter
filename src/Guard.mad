import type { Maybe } from "Maybe"

import Log from "@/Log"



// b = context
// a = event
export type Guard pay ctx = Guard(String -> Maybe ctx -> Maybe pay -> Boolean)
export create = pipe(
  Guard,
  Log.env.guard("created"),
)
check :: String -> Maybe ctx -> Maybe pay -> Guard pay ctx -> Boolean
export check = (ev, ctx, pay, guard) => where(guard) {
  Guard(engarde) =>
    engarde(ev, ctx, pay)
}
