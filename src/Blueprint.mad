import type { BlueprintDict, Draft } from "@/Blueprint/Types"

import String from "String"

import D from "@/Blueprint/Draft"
import { Draft } from "@/Blueprint/Types"



export type Draft
export Draft
export draft = Draft
export buildDraft = D.build

export type Blueprint a p x = InvalidBlueprint(String) | Blueprint(BlueprintDict a p x, x)
