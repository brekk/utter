import type { Either } from "Either"
import type { Maybe } from "Maybe"

import type { Machine } from "@/Machine"
import type { State } from "@/State"
import type { Transition, Transitions } from "@/Transition"

import { Left, Right } from "Either"

import { Draft, InvalidDraft } from "@/Blueprint/Draft"
import { log } from "@/Log"
import Machine from "@/Machine"



export type Blueprint a b c
  = Blueprint(
    List String,
    String -> Boolean,
    String -> Boolean,
    Dictionary (State String) (Transitions String b c),
  )

/*
fromMachine :: Machine String a -> Blueprint a b c
export fromMachine = (m) => pipe(
  Draft.fromMachine,
  fromDraft(Machine.initial(m)),
)(m)
*/
