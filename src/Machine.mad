import type { Maybe } from "Maybe"
import type { Set } from "Set"

import type { State } from "@/State"

import Dict from "Dictionary"
import Fn from "Function"
import List from "List"
import Set from "Set"
import String from "String"

import Event from "@/Event"
import State from "@/State"
import Transition from "@/Transition"
import Tuple from "@/Tuple"



export type Machine pay ctx = Machine(String, List (State pay ctx), Maybe (State pay ctx))
// machine: "pen"
//   ↳ state: "extended"
//       ↳ event: "click" → "contracted"
//   ↳ state: "contracted"
//       ↳ event: "click" → "extended"

create :: String -> List (State pay ctx) -> Machine pay ctx
export create = (s, x) => Machine(s, x, List.first(x))
name :: Machine pay ctx -> String
export name = where {
  Machine(n, _, _) =>
    n
}
rawStates :: Machine pay ctx -> List (State pay ctx)
export rawStates = where {
  Machine(_, sts, _) =>
    sts
}
states :: Machine pay ctx -> Set String
export states = pipe(
  rawStates,
  map(State.name),
  Set.fromList,
)
hasState :: String -> Machine pay ctx -> Boolean
export hasState = (x, mch) => pipe(
  states,
  Set.includes(x),
)(mch)
state :: Machine pay ctx -> Maybe (State pay ctx)
export state = where {
  Machine(_, _, m) =>
    m
}
events :: Machine pay ctx -> Set Event
export events = pipe(
  rawStates,
  chain(
    pipe(
      State.transitions,
      map(Transition.focus),
    ),
  ),
  Set.fromList,
)
hasEvent :: String -> Machine pay ctx -> Boolean
export hasEvent = (ev, mch) => pipe(
  events,
  Set.includes(ev),
)(mch)
getEvent :: String -> Machine pay ctx -> Maybe String
export getEvent = (ev, mch) => pipe(
  events,
  Set.filter(Fn.equals(ev)),
  Set.toList,
  List.first,
)(mch)
eventMap :: Machine pay ctx -> Dictionary String (Dictionary String String)
export eventMap = pipe(
  rawStates,
  map(
    pipe(
      State.toPair,
      Tuple.mapSnd(
        pipe(
          map(Transition.toSimplePair),
          Dict.fromList,
        ),
      ),
    ),
  ),
  Dict.fromList,
)
